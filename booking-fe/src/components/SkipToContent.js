import React from 'react';

// Accessibility skip link — hidden until focused via Tab, lets keyboard users jump past navigation.
const SkipToContent = () => (
    <a href="#main-content" className="skip-to-content-link">Bo qua phan dieu huong</a>
);

export default SkipToContent;
